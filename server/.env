# ------------------------
# 환경 모드
# ------------------------
ENV=dev

# ------------------------
# Emotion Service
# ------------------------
EMOTION_API_URL=http://localhost:9000/v1/predict
EMOTION_TIMEOUT_S=2.0

# ------------------------
# GPT / OpenAI API
# ------------------------
GPT_API_URL=https://api.openai.com/v1/chat/completions
GPT_MODEL=gpt-4o-mini
GPT_API_KEY=REPLACE_WITH_YOUR_KEY   # 반드시 실제 키로 교체하세요

# ------------------------
# 공통 설정
# ------------------------
REQUEST_TIMEOUT_S=6.0
MAX_INPUT_CHARS=1000
CORS_ORIGINS=["*"]

# ------------------------
# 보안 (선택: 둘 중 하나 이상 권장)
# ------------------------
# API Key 인증 (클라이언트가 X-Api-Key 헤더로 전달해야 함)
SERVER_API_KEY=REPLACE_WITH_API_KEY

# HMAC 인증 (더 안전)
SERVER_HMAC_SECRET=REPLACE_WITH_LONG_RANDOM_SECRET
HMAC_ALLOWED_SKEW=300   # 허용 타임스큐(초), 기본 5분

# ------------------------
# 레이트리밋 (기본 분당 60회)
# ------------------------
RL_DEFAULT_RATE=60
RL_DEFAULT_PER=60
